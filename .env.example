# Gemini requests must go through the backend. Never put this in frontend/VITE_ variables.
GEMINI_API_KEY=

# Google Maps browser key: restrict to the intended site origins in Google Cloud.
VITE_GOOGLE_MAPS_API_KEY=

PORT=3001
